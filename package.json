{
  "name": "github-actions-pipeline-demo",
  "version": "1.0.0",
  "description": "Demo application for GitHub Actions pipeline",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ && cd dist && zip -r ../artifact.zip .",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "author": "",
  "license": "MIT"
}
